const { createConversation } = require("@grammyjs/conversations")
const { Context } = require("grammy")


/**
 * @param {Context} conversation
 * @param {Context} ctx
 */
async function createCharacter(conversation, ctx) {
}

module.exports = createCharacter